# Soline — מצב-חי לחידוש (עדכון: 31 אוג׳ 2026, סוף-יום)

> **איך ממשיכים מחר:** בצ'אט חדש כתוב **"תמשיך מ-SESSION_STATE"**. הכל בקבצים (D:\Soline) + git (PoratMeasurements/soline-cad) + זיכרון-הקבע — שום דבר לא-אבד.

## גרסה נוכחית: **v0.8.6.9** (versionCode 22) · DB v21
- סכמה: **4-חלקים 0.8.6.x** (גרסאות-ניסיון, מקדמים ספרה-אחרונה). כל שחרור → commit+push לפורת + `G:\My Drive\Soline-APK\soline-measure_v0.8.6.9.apk`.
- ⚠️ **המודד לא-התקין עדיין את 0.8.6.7/8/9** — שיתקין את **0.8.6.9** ויעבור על התור.

## מה נבנה היום (מהחדש לישן)
- **v0.8.6.9** — 🔄 **שחזור פרויקט מ-Drive** (`SolReader` + `repo.importSol` + כפתור בדף-הבית · 181005) · ולידציית-גובה-מינימלי (010207) · חיווי-שיתוף-באגים-בין-מכשירים בהגדרות (212824).
- **v0.8.6.8** — **איחוד-השרטוט:** אין עוד "לייזר-קטן"/"מטר" נפרדים במנוע — שיטה-אחת **שרטוט** (קולט לייזר D2/בוש + מטר, פר-קטע). WALLS = "פתח שרטוט" + רשימת-קירות (121524/192221/213532/191959/191918).
- **v0.8.6.7** — **עריכת כל-פרטי-הפרויקט** (קישור Project↔Job · מיגרציה 21 · 195918) · הערה-בלבד-מדיאלוג (120539/192750) · סטיית-גובה מוצגת (115901) · דדופ גבהים/שינויים למסכים-ייעודיים (192617/213617) · כפתור-בית לסרגל-הכלים (192044).
- **v0.8.6.6** — זום/הזזה בקנבס (213400) · הסקיצה מציגה שרטוט-קיים (192339) · גרירת-משגר-חסומה (205033).
- **v0.8.6.3–5** — מסלול-מודד-מונחה 8-תחנות · הערות-per-אלמנט · מין/מקס-גובה · מידות-קבועות (120826) · X6 חזית · הרבה באגי-שדה.

## המנוע (הליבה)
- **מסך-אחד: `ui/unified/UnifiedMeasureScreen.kt`** — `RoomPlanCanvas` **מתמיד** (זום+הזזה) תמיד מציג `repo.walls`. תחנות: גישה→שיטה→קירות→חזיתות→אלמנטים→תמונות→3D→סגירה.
- **שיטה-אחת: שרטוט** (`ui/draw/SketchInjectScreen.kt` · route `sketch/{rid}`) — פינות→בחר-קיר→הזרק לייזר(כל-מכשיר)/ידני. מוסיף-לקיים (לא-מוחק). + P2P.
- פילוס/גבהים/שינויים = תחנת-שיטה → מסכים-ייעודיים (`heights/`,`changes/`,`floor/`,`ceiling/`).

## מרכז-הבאגים (דו-כיווני דרך Drive · [[soline-retest-gate]])
- קבצים ב-`G:\My Drive\קבצים ללמידת מכונה\באגים מסוליין\`: `bug_status.json` (סטטוס+serial) · `retest_queue.json` · `bug_*.json/png` · `retest_response_*`.
- **מצב: 51 סגורים · 22 fixed-בתור-אימות · 6 בעבודה.**
- ⚠️ **סוכן-סטטוס-רקע** משכתב את הקבצים לפעמים ומחזיר fixed→working. אחרי ריצת-רקע — ודא שהתור לא-איבד fixed-ממתינים.

## פתוח למחר — **דורש חומרה/מכשיר (לא-לבנות-עיוור):**
1. **220942** חיבור-מכשירים במנוע · **221536** זוויות X6/DST (Measure-3D) · **191619** D2-מחוץ-לטווח — **צריך את הלייזרים הפיזיים מול המודד.**
2. **210546** VERIFY+FIT — ממומש (VerificationScreen + חבילת-fit); נשאר ליטוש לפי-סוג-חדר.
3. **191151** סקיצה: זווית-ידנית + אלכסון-פיתגורס.
4. מדיה בשחזור-‏.sol‏ (תמונות/וידאו) — SolReader משחזר גאומטריה+אלמנטים, לא-מדיה.

## בנייה/שחרור (תזכורת)
- מירור PowerShell: `robocopy "D:\Soline\app-measure" "C:\android-dev\soline-measure" /MIR /XD .gradle build .idea /XF desktop.ini local.properties`
- build: `$env:JAVA_HOME="C:\android-dev\jdk-17.0.20+8"; gradle `:app:assembleDebug`. מיגרציה חדשה → `kspDebugKotlin --rerun-tasks` להחזיר schema + `testDebugUnitTest --tests *Migration*`.
