# סקירת-בקורת אפליקציה — 25 אוג׳ 2026 (`/qa`)

7 סקירות (UX/UI · באגים/קריסות · QA-פונקציונלי · זרימת-מודד · P2P · חזית+גליות · סקר-מפלסים). ~80 ממצאים.
מדורג: 🔴 קריטי · 🟠 גבוה · 🟡 בינוני · ⚪ נמוך.

---

## 🔴 קריטי — לתקן ראשון

### קבוצה A — אובדן-נתונים (הכי-דחוף)
- 🔴 **התנגשות-שמות-מדיה → דריסת-תמונות בשקט** — כל התמונות בתיקייה-שטוחה; `חזית-1_01.jpg` של חדר-A נדרס ע"י חדר-B. גם ״חדר_NN״ וגם מדיית-פרויקט. **אובדן תמונות-שדה בכל שימוש רב-חדרי.** `PhotoCapture.kt:328-335`, `VideoCapture.kt:163-168`. תיקון: להוסיף roomId/wallId/projectId לשם-הקובץ.
- 🔴 **`deletePhoto`/`deleteVideo` מוחקים קובץ-משותף** — מוחק את הקובץ שרשומה-אחרת מצביעה אליו. `Repo.kt:218-236`.
- 🔴 **`fallbackToDestructiveMigration()`** — אי-התאמת-סכמה → מחיקת-כל-הנתונים בשקט, בלי גיבוי. `SolineDatabase.kt:449`. להסיר מ-release.
- 🟠 **מדיית-פרויקט מתייתמת** — אין FK על `projectId`; `deleteProject` לא מוחק → רשומות+קבצים דולפים. `Entities.kt:207-273`.

### קבוצה B — מספרים שגויים לנגר
- 🔴 **"קלוט גובה מהלייזר" שומר מרחק-גלם, לא גובה-מהרצפה** — לייזר לשקע נותן 2800(מרחק) כגובה. `WallElevationUnified.kt:1083,1163,753`. לתקן דרך `laserHeightMm(d,θ)`.
- 🔴 **`laserHeightMm` נופל למרחק-גלם בירי-אופקי** — נקודה בגובה-עין 3מ' → "גובה 3000". `WallElevationUnified.kt:190-199`.
- 🔴 **datum-תחתון = הנקודה-הנמוכה-שנדגמה, לא הרצפה** — אם לא יורים ברצפה, כל הגבהים מוסטים. `WallProfileSolver.kt:142-149`.
- 🔴 **עומק D מיוצא 0 בשקט** — `defaultDepth` (מקרר 650, מזגן 200) לא מגיע לשדה. `ElementMeasureFields.kt:72-83`.
- 🔴 **אין דגל "נמדד מול ברירת-מחדל"** — הנגר לא מבחין מדידה-אמיתית מניחוש-קטלוג (קירות פתרו עם `heightMeasured`). `ElementMeasureFields.kt:256`, `Entities.kt:144`.

### קבוצה C — שיבוש-סקר
- 🔴 **P2P: עריכת-פינה-אמצעית משבשת את הפוליגון** — remove+append → חדר-מצטלב. `P2PMeasureScreen.kt:306,147`.
- 🔴 **P2P: ירי בלי-DST → כל-הפינות על-קו** — לא-חסום. `P2PMeasureScreen.kt:237`, `StationSolver.kt:166`.
- 🔴 **P2P: CW מול CCW → תוכנית-מראה** — אין toggle בשטח. `StationSolver.kt:162-168`.
- 🔴 **מפלסים: מחיקת-נקודה-אמצעית → התנגשות-idx** — idx לפי-count. `LevelSurveyScreen.kt:72-93`.
- 🔴 **מפלסים: ירי-בלי-זווית → Z-זבל בשקט** — נקודת-אפס בלי-זווית שומרת מרחק-גלם. `FloorLevelSolver.kt:60`, `LevelSurveyScreen.kt:89`.

### קבוצה E — קריסות בנתיב-הייצוא (מצלמה/וידאו)
- 🔴 **ייצוא-ה-.sol רץ על main-thread → ANR** — כל בניית-ה-ZIP (כולל קריאת-תמונות/וידאו) על ה-thread-הראשי. `AppUi.kt:290-308`. תיקון: `withContext(Dispatchers.IO)`.
- 🔴 **פענוח-Bitmap מלא + סיבוב → OutOfMemory** — תמונת-פורטרט (סיבוב-EXIF) = 2 bitmaps מלאים (~48MB כ"א) → קריסה אחרי-צילום. `PhotoCapture.kt:354-375`. תיקון: inSampleSize/downscale + recycle.
- 🔴 **`readBytes()` טוען וידאו-שלם לזיכרון → OOM** — וידאו 2-דק' ~200MB במערך-אחד. `SolWriter.kt:148,153`. תיקון: stream (`copyTo`) כמו `BackupManager`.
- 🟡 לולאת-JSON-לא-מוגנת מפילה-ייצוא-שלם על נקודה-פגומה · snapshot-ייצוא לא-טרנזקציוני.

### קבוצה D — UI שבור/no-op
- 🔴 **כפתור "סגור פרויקט" הוא no-op** — רק `popBackStack()`, בלי closeProject/ייצוא. `CloseRoomScreen.kt:218`.
- 🔴 **אין שער-ולידציה לאלמנטים** — מידה ריקה/0 נשמרת; מצב-מרכז → `fromLeft` שלילי. `ElementMeasureFields.kt:100-116`.

---

## 🟠 גבוה
- ייצוא לא-חסום ע"י שלמות (השער כתוב ל-.sol אך לא-חוסם) + פרויקט-0-חדרים מייצא ריק. `AppUi.kt:317`, `SolWriter.kt:680`.
- וידאו write-only — אי-אפשר לצפות/למחוק סרטון (והם חובה-לשער). `VideoCapture.kt`.
- מניעת-הרשאת-מצלמה שקטה — כפתור-מת בלי-משוב. `PhotoCapture.kt:183`.
- אי-אפשר לערוך/למחוק קיר-פנימי (רק undo-LIFO) · אי-אפשר למחוק אביזר ממסך-החזית.
- P2P: "נקה הכל"/"חזרה" בלי-אישור · אין backsight/זיהוי-הזזת-תחנה · arm יכול להיתקע.
- מפלסים: arm שורד ניתוק-BLE → ירי-לתא-שגוי · אין datum/מישור-מיטבי/סיכום גבוה-נמוך-טווח.
- חזית: גובה-סוֹפית לא-נשמר · "נקה" בלי-אישור · חזרה מוחקת-מסגרת · סימן-גליות יכול להתהפך בלי-flip.
- height-sweep דורס גבהי-קיר-פרטניים שנמדדו. `Repo.kt:71`.
- מחיקת-קיר לא מעדכנת `entranceWallIdx`/זווית-פנייה.

## 🟡 בינוני (נבחרים)
- `openMode` לא מסונן לפי-סוג · ציר/כיוון-חלונות לא-נלכד · פרופיל-u לא-מונוטוני → צללית-מצטלבת · מדיה בלי roomId ב-.sol · פינות-כפולות = קיר-אפס.
- **מערכת-עיצוב `SolineComponents.kt` קיימת אך מסכי-השדה מתעלמים ממנה** → כל מסך ממציא כפתורים/כותרות (פיצול-עיצובי).
- חצי-"חזרה" לא-עקביים (אחד לא-ממוראר ל-RTL) · שורת-3-כפתורים נחתכת בטלפון-צר.

## ⚪ נמוך
- מלל "טיוטה-לאישור-עו"ד" מוצג-למשתמש · אין מצבי-טעינה · אמוji בלי-נגישות · פינת-רפלקס מציגה-זווית-שגויה.

---

## ✅ מה תקין (אומת)
מתמטיקת-התחנה (`d·sinθ`) נכונה · FK+cascade מוצהרים נכון לרוב-הישויות · `.sol` layers/manifest עקביים · `reindexWallsAfter` בלי off-by-one · nullable-פתחים מטופל · seq-extraction חסין.

## הערה
`ElementCatalog.all` ≈ 97 סוגים (לא 44 — מספר-ישן). לא-באג.
